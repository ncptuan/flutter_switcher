// import 'package:fitplus_operation/model/view/master_data_model.dart';

import 'package:flutter/material.dart';

class Constants {
  static const switchAnimationDuration = Duration(seconds: 1);
  static const auraAnimationDuration = Duration(seconds: 5);
  static const planetOffset = Offset(160, -360);
  static const numberOfStars = 20;
}
